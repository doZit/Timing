<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.timing">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Timing"
        tools:replace="android:appComponentFactory"
        android:appComponentFactory="任意字符">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".publish.PublishActivity"/>
        <activity android:name="Detail_Card"/>
        <activity android:name=".plant.Community_Page"/>
        <activity android:name=".account.LinkList"/>
        <activity android:name=".account.SelfPage"/>
        <activity android:name=".message.CreateTalk"/>
        <activity android:name=".publish.SelectPhoto"/>
        <activity android:name=".message.TalkActivity"/>
        <activity android:name=".VedioMainActivity"/>
    </application>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>
</manifest>